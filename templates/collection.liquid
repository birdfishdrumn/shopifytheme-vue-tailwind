  {% comment %} <h1 class="text-center font-bold my-8 text-3xl">{{ collection.title }}</h1>

  {% render 'product-card' %} {% endcomment %}
<div class="container mx-auto max-w-4xl">
  <div class="collection">
    <h1 class="collection-title">{{ collection.title }}</h1>
    <div class="grid gap-10 grid-cols-3 sm:grid-cols-2">
      {% render 'product-card' for collection.products as product %}
    </div>
  </div>
</div>
